<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="taxi_manager.list">
      <field name="name">taxiManager list</field>
      <field name="model">taxi_manager.taxi_manager</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="taxi_manager.action_window">
      <field name="name">taxiManager window</field>
      <field name="res_model">taxi_manager.taxi_manager</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="taxi_manager.action_server">
      <field name="name">taxiManager server</field>
      <field name="model_id" ref="model_taxi_manager_taxi_manager"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="taxiManager" id="taxi_manager.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="taxi_manager.menu_1" parent="taxi_manager.menu_root"/>
    <menuitem name="Menu 2" id="taxi_manager.menu_2" parent="taxi_manager.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="taxi_manager.menu_1_list" parent="taxi_manager.menu_1"
              action="taxi_manager.action_window"/>
    <menuitem name="Server to list" id="taxi_manager" parent="taxi_manager.menu_2"
              action="taxi_manager.action_server"/>
    -->
  </data>
</odoo>